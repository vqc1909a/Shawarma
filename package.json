{
	"name": "shawarma",
	"version": "1.0.0",
	"main": "index.js",
	"license": "MIT",
	"private": true,
	"workspaces": [
		"backend",
		"frontend"
	],
	"scripts": {
		"test:backend": "yarn workspace backend test:coverage",
		"test:frontend": "yarn workspace frontend test:coverage",
		"dev": "yarn workspace backend dev && yarn workspace frontend dev",
		"build:local": "yarn workspace frontend build && yarn workspace backend start:local",
		"build": "yarn install && yarn workspace frontend build && yarn workspace backend start"
	}
}
